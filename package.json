{
  "name": "ah-pure-md5",
  "version": "1.0.1",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "module": "dist/index.js",
  "scripts": {
    "ci": "prettier --check src && tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json",
    "prepublishOnly": "npm run ci && npm run build",
    "release": "npm run ci && release-it"
  },
  "author": "concefly@foxmail.com",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.0.3",
    "prettier": "^3.0.2",
    "release-it": "^16.1.5"
  },
  "sideEffects": false
}
